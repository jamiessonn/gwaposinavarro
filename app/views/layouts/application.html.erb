<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "2do_ls" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
 
  <body>
     <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <!-- Title Area -->
        <li class="name">
          <h1>
            <a href="/">
              2do_ls
            </a>
          </h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#"><span>menu</span></a></li>
      </ul>
   
      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
          <li class="divider"></li>
          <li><a href="/tasks">Tasks</a></li>
          <li class="divider"></li>
          <li><a href="/help">Help</a></li>
          <li class="divider"></li>
          <% if current_user %>
            <li><a href=""><%= current_user.email %></a></li>
            <li class="divider"></li>
            <li><%= link_to "Goodbye", logout_path %></li>
          <% else %>
            <li><%= link_to "Join My Kingdom", signup_path %></li>
            <li class="divider"></li>
            <li><%= link_to "Log In", login_path %></li>
          <% end %>
          <li class="divider"></li>
        </ul>
      </section>
    </nav>
    
  <section id="wrap" role="document">
    <div class="row">
      <div class="large-12 columns">
        <% flash.each do |name, msg| %>
          <% if msg.is_a?(String) %>
            <div data-alert class="alert-box radius <%= name == :notice ? "success" : "alert" %>">
              <%= content_tag :div, msg %>
              <a href="#" class="close">&times;</a>
            </div>
          <% end %>
        <% end %>
        <%= yield %>
      </div>
    </div>
  </section>
  <footer id="footer">
    <hr>
                      <div align="center"><p>&copy; Alonswagger de Leon & Jamiesstrong Carlo Lao 2014</p>GWAPO SI NAVARRO</div>
  </footer>
    <%= javascript_include_tag "application" %>
  </font>
  </body>
</html>